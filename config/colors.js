const colors = require('colors');

const temasPersonalizados = colors.setTheme({
    silly: 'rainbow',
    info: 'green',
    warn: 'yellow',
    error: 'red',
    res: 'blue'
});

module.exports = temasPersonalizados